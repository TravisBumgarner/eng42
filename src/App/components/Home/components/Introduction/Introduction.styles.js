import styled from 'styled-components'

import { FONT_SIZE_LARGE } from 'Theme'

const IntroText = styled.p`
    margin: 20px 0;
    font-size: ${FONT_SIZE_LARGE};
`

const SkillText = styled.span`
    font-weight: 400;
`

export { IntroText, SkillText }
