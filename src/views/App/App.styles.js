import styled from 'styled-components';

const AppWrapper = styled.div`
  font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
  font-size: 20px;
  color: purple;
  background-color: green;
`;

export {
  AppWrapper, //eslint-disable-line
}